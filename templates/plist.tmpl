<section>
<table class="table">
  <thead>
    <th>Nr</th>
    <th>Imię</th>
    <th>Nazwisko</th>
    <th>Akcja</th>
  </thead>
  <tbody>
    {{ range .pat }}
      <tr >
        <th>
          {{ .Id }}
        </th>
        <td>{{.Name}}</td>
        <td>{{.Surname}}</td>
        <td><a href="{{.GetLink}}">Pokaż</a></td>
      </tr>
    {{end}}
  </tbody>
</table>
</section>
